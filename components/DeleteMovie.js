import React from 'react';


const Delete = props => {
    const deleteHandler = () => {
        
    }

    return (
        <button onClick={deleteHandler} >Delete</button>
    );
};

export default Delete;